---
# tasks file for apache
- name: Ensure that the httpd package is not installed on the system
  yum: 
    name: httpd
    state: absent
- name: Ensure that the httpd packge is not installed on the System
  yum:
    name: httpd-tools
    state: absent
